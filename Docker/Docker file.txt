# Use an official web server image as the base image
FROM nginx

# Copy your HTML files to the default web server directory
COPY ./your-html-directory /usr/share/nginx/html

# Expose the default web server port
EXPOSE 8085
